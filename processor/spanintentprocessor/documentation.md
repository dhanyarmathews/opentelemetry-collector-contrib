[comment]: <> (Code generated by mdatagen. DO NOT EDIT.)

# spanintent

## Internal Telemetry

The following telemetry is emitted by this component.

### otelcol_processor_spanintent_errors_total

Total number of errors encountered during span intent processing.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {errors} | Sum | Int | true |

#### Attributes

| Name | Description | Values |
| ---- | ----------- | ------ |
| error_type | The type of error encountered during processing. | Any Str |

### otelcol_processor_spanintent_new_trace_id_received

Number of new unique trace IDs received by the span intent processor.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {traces} | Sum | Int | true |

### otelcol_processor_spanintent_processing_duration

Duration of the span intent processor's main processing loop (processBufferedSpans).

| Unit | Metric Type | Value Type |
| ---- | ----------- | ---------- |
| ms | Histogram | Double |

### otelcol_processor_spanintent_sampled_cache_hits

Number of hits in the sampled traces cache.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {spans} | Sum | Int | true |

### otelcol_processor_spanintent_sampled_cache_misses

Number of misses in the sampled traces cache.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {spans} | Sum | Int | true |

### otelcol_processor_spanintent_sampling_decision_latency

Latency of making a sampling decision for a single trace

| Unit | Metric Type | Value Type |
| ---- | ----------- | ---------- |
| us | Histogram | Double |

### otelcol_processor_spanintent_spans_received

Number of spans received by the span intent processor.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {spans} | Sum | Int | true |

### otelcol_processor_spanintent_trace_buffer_size

Current number of unique trace IDs in the internal buffer.

| Unit | Metric Type | Value Type |
| ---- | ----------- | ---------- |
| {spans} | Gauge | Int |

### otelcol_processor_spanintent_traces_classified_total

Total number of traces classified by intent.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {traces} | Sum | Int | true |

#### Attributes

| Name | Description | Values |
| ---- | ----------- | ------ |
| classification_category | The classification category assigned to the trace (e.g., critical, normal, etc.) | Any Str |

### otelcol_processor_spanintent_traces_processed

Number of traces processed by the span intent processor.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {traces} | Sum | Int | true |

### otelcol_processor_spanintent_traces_sampled

Number of traces sampled by the span intent processor.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {traces} | Sum | Int | true |

#### Attributes

| Name | Description | Values |
| ---- | ----------- | ------ |
| sampling_category | The category of the sampling decision (e.g., head, tail, etc.) | Any Str |

### otelcol_processor_spanintent_traces_unsampled

Number of traces unsampled by the span intent processor.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {traces} | Sum | Int | true |

#### Attributes

| Name | Description | Values |
| ---- | ----------- | ------ |
| sampling_category | The category of the sampling decision (e.g., head, tail, etc.) | Any Str |

### otelcol_processor_spanintent_unsampled_cache_hits

Number of hits in the unsampled traces cache.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {spans} | Sum | Int | true |

### otelcol_processor_spanintent_unsampled_cache_misses

Number of misses in the unsampled traces cache.

| Unit | Metric Type | Value Type | Monotonic |
| ---- | ----------- | ---------- | --------- |
| {spans} | Sum | Int | true |
